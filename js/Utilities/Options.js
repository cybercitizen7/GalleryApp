export default class Options {
    constructor(container, api, loopPanel, startIndex) {
        this.container = container;
        this.api = api;
        this.loopPanel = loopPanel;
        this.startIndex = startIndex;
    }
}